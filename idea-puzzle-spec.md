# 思想拼圖（Idea Puzzle）— 產品規格書

> 版本：0.1（草稿）
> 日期：2026-08-05
> 狀態：訪談完成、待確認
> 語言：本產品 UI 與輸出均以**繁體中文**為主

---

## 1. 專案概述

「思想拼圖」是一個**純前端靜態網頁應用程式**，幫助使用者**收集零散的想法片段**，透過 **AI 分析、分群、整合**，將片段組合成**完整、可執行的成果**（行動計畫、提案、文章、研究報告或路線圖）。

核心隱喻：**思想像散落的拼圖碎片**，系統負責分類與引導，AI 負責把碎片拼成完整的圖畫。

```
收集碎片 → 分群分類 → AI 整合 → 產出成果（可匯出/分享）
(Capture)   (Cluster)  (Synthesize)  (Output)
```

---

## 2. 背景與動機

- 使用者腦中常同時存在多個不完整、互不相關的想法片段，若不及時記錄容易遺失。
- 市面上工具分兩極：**捕捉快的（如 Apple Notes、Mem）整合能力弱**；**整合強的（如 Heptabase、Notion）捕捉流程太重**（研究確認的「兩工具取捨」痛點）。
- 使用者希望單一工具同時解決「快速捕捉」與「AI 深度整合」兩件事，且**完全免費、不需要建置後端**。

---

## 3. 目標與非目標

### 3.1 目標（Goals）
- 讓使用者**用任何方式**快速記錄想法片段（打字、語音、摘錄、圖片、檔案）。
- 一鍵觸發 AI 整合，把片段**自動分群**並組合成**指定型態的完整成果**。
- 所有資料**存在本機檔案**（Markdown + JSON），開放格式、可攜帶、可備份。
- 全程**零費用**（使用免費 AI API 額度）。
- 產出成果**可追溯**（成果引用來源片段）。

### 3.2 非目標（Non-Goals，本版本不做）
- 不支援多人協作／帳號系統。
- 不建置後端伺服器（金鑰直接在瀏覽器中，個人使用可接受）。
- 不做完整的「拼圖過程」動畫視覺化（只要簡單分群分類即可）。
- 不做雲端同步（僅手動匯出/匯入）。
- 不做行動 App（但需支援手機瀏覽器可用）。

---

## 4. 使用者與使用情境

- **主要使用者**：個人（本機使用，偶爾把成果匯出給別人看）。
- **典型情境**：
  1. 走路時想到新點子 → 用語音快速記錄一句話。
  2. 讀文章看到好段落 → 摘錄貼上，或貼上網址。
  3. 看書時拍了一張圖表 → 上傳圖片。
  4. 累積一段時間後 → 按「開始拼圖」，選擇想要產出的類型 → 一鍵獲得完整成果。
  5. 把成果匯出成 Markdown/PDF 分享給朋友或同事。

---

## 5. 核心工作流（五步驟）

1. **捕捉（Capture）**：透過多種管道新增片段。
2. **整理（Organize）**：片段清單、標籤、搜尋、重複標示。
3. **分群（Cluster）**：AI 將片段自動分成數個主題群組（**整合前先展示群組給使用者看**）。
4. **整合（Synthesize）**：AI 依使用者選擇的**成果型態**，將所有片段整合為完整輸出，**衝突處標記並提供兩種詮釋**，成果標示**引用來源**。
5. **產出（Export）**：檢視、複製、匯出成檔案分享。

---

## 6. 功能需求

### 6.1 片段收集（Capture）

| 編號 | 功能 | 細節 |
|---|---|---|
| F1.1 | 手動文字輸入 | 最核心功能；支援一行或長文 |
| F1.2 | 語音轉文字 | 瀏覽器 `MediaRecorder` 錄音 → 呼叫免費 STT API（見 §8）轉成文字片段 |
| F1.3 | 貼上網址/文章摘錄 | 純記錄連結與文字，**不做**自動抓取網頁內容（免費 API 限制，V2 可考慮） |
| F1.4 | 圖片/截圖上傳 | 支援常見格式（PNG/JPG/WebP）；AI 分群/整合時透過**視覺模型**理解圖片內容 |
| F1.5 | 檔案上傳 | PDF、文字檔等；PDF 用客戶端 **pdf.js** 解析後以文字形式儲存 |
| F1.6 | 從現有筆記匯入 | 支援匯入 Markdown 檔案（每檔或每個標題拆成片段）與 JSON 備份檔 |
| F1.7 | 快速捕捉 UI | 主畫面常駐輸入框；語音按鈕一鍵錄音 |

### 6.2 片段管理（Organize）

| 編號 | 功能 | 細節 |
|---|---|---|
| F2.1 | 片段清單 | 依時間排序，可搜尋（關鍵字） |
| F2.2 | 標籤 | 每片段可加標籤（可選功能，不強制） |
| F2.3 | 編輯/刪除 | 可修改內容、刪除片段 |
| F2.4 | 重複偵測 | AI 在整合時**偵測相似片段並標示**「這些片段很相似」，由使用者決定是否合併（**不自動合併**） |
| F2.5 | 手動歸類 | 使用者可手動把片段拖到某個群組（進階選項） |

### 6.3 拼圖整合（Synthesize）

| 編號 | 功能 | 細節 |
|---|---|---|
| F3.1 | 手動觸發 | 使用者按「開始拼圖」按鈕才觸發（**不做**自動/定時觸發） |
| F3.2 | 成果型態選擇 | 整合前選擇輸出型態：**行動計畫/待辦清單、提案/企劃書、文章/簡報內容、研究報告/知識彙整、路線圖/藍圖**，或「不固定，依內容決定」 |
| F3.3 | 自動分群 | AI 先將所有片段分為數個主題群組，並**展示群組分類給使用者確認**（簡單分類即可，無需動畫） |
| F3.4 | 一鍵全自動整合 | 確認分群後，AI **全自動一次完成**整合（無逐步確認） |
| F3.5 | 衝突處理 | 片段矛盾時，AI 在成果中**標記衝突點，並同時提供兩種詮釋**供使用者選擇 |
| F3.6 | 引用來源 | 成果每個段落標示引用自哪些片段（可點回片段原文） |
| F3.7 | 過程記錄 | 每次整合的設定、分群結果、原始片段快照一併儲存（可重播/重跑） |
| F3.8 | 部分選擇 | 可只選部分片段（或某些群組）進行整合，不必每次全量 |

### 6.4 成果與分享（Output）

| 編號 | 功能 | 細節 |
|---|---|---|
| F4.1 | 成果檢視 | 以 Markdown 渲染檢視成果 |
| F4.2 | 匯出檔案 | 匯出 **Markdown / PDF / 純文字**（PDF 用瀏覽器列印功能） |
| F4.3 | 複製 | 一鍵複製全文或部分內容 |
| F4.4 | 成果存檔 | 成果自動存為本機檔案，歷史成果可重新檢視 |

---

## 7. 資料模型與儲存

儲存原則：**本機檔案，開放格式**。使用瀏覽器的 **File System Access API**（Chrome/Edge）讓使用者選擇一個資料夾，應用程式直接讀寫；不支援時退回「匯出/匯入 JSON」方案。

```
<user-selected-folder>/
├── fragments/
│   ├── 20260805-1030-abc123.md   # 每個片段一個 Markdown 檔
│   └── ...
├── groups/
│   └── <session-id>.json         # 分群結果
├── outputs/
│   ├── <output-id>.md            # 整合成果
│   └── ...
├── index.json                    # 片段索引（元資料、標籤、關聯）
└── settings.json                 # API 設定（不含金鑰）、偏好
```

**片段（Fragment）結構**：
```json
{
  "id": "20260805-1030-abc123",
  "type": "text | voice | link | image | file",
  "content": "片段的文字內容（圖片/語音/檔案轉換後）",
  "source": { "kind": "manual | voice | link | image | upload | import",
              "originalFile": "voice-1.webm | photo.png | paper.pdf" },
  "tags": ["工作", "靈感"],
  "createdAt": "2026-08-05T10:30:00+08:00",
  "updatedAt": "2026-08-05T10:30:00+08:00",
  "duplicatesOf": ["<fragment-id>", "..."],
  "status": "open | grouped | synthesized"
}
```

**分群（Group）結構**：群組名稱、群組摘要、成員片段 ID 清單、整合用狀態。
**成果（Output）結構**：型態、全文 Markdown、`citations: [{段落/主張, 片段 ID}]`、衝突清單 `conflicts: [{片段A, 片段B, 說明, 詮釋1, 詮釋2}]`、產生時間。

> **設計決策待確認**：File System Access API 目前僅 Chrome/Edge 完整支援（Safari 部分、Firefox 無）。若使用者常用 Safari/Firefox，需改以「匯出/匯入」為主要方案。見 §12 開放問題。

---

## 8. AI 整合設計

### 8.1 服務選擇（研究結論）

| 服務 | 免費額度 | 視覺 | 語音轉文字 | 瀏覽器直呼 | 用途 |
|---|---|---|---|---|---|
| **Google Gemini**（推薦主用） | Flash 系列 ~1,500 次/日 | ✅ | ✅（音訊輸入） | ✅ CORS 開放 | 分群、衝突偵測、整合、圖片理解 |
| **Groq** | 30 RPM / 14,400 次/日 | ❌ | ✅ Whisper-large-v3 | ✅ CORS 開放 | 語音轉文字（替代方案） |
| **OpenRouter**（備援） | `:free` 模型 50 次/日 | 依模型 | ❌ | ✅ CORS 開放 | 備援/其他模型需求 |

- **主要建議**：以 **Google Gemini 免費層**為單一主要供應商（文字+視覺+音訊一次滿足），**Groq Whisper** 作為語音轉文字的首選或備援。
- 全部支援瀏覽器 `fetch()` 直接呼叫，**不需要後端**，符合「純靜態」約束。

### 8.2 API 金鑰管理

- 金鑰由使用者自行申請，於設定頁輸入，存於 **localStorage**（**不硬編碼進程式碼**）。
- **已知風險**：瀏覽器直呼 API 時金鑰可見於開發者工具；對個人自用工具可接受。規格中須於設定頁顯示此警告。
- 若未來想公開部署給他人使用，需加 Cloudflare Worker 代理（列為 V2 選項，**非本版本範圍**）。

### 8.3 模型分工建議

| 任務 | 建議模型 | 理由 |
|---|---|---|
| 語音轉文字 | Groq `whisper-large-v3`（或 Gemini 音訊） | 免費、快 |
| PDF 解析 | 客戶端 pdf.js（不佔 API） | 免費、隱私 |
| 分群/去重/衝突偵測 | Gemini Flash-Lite | 便宜、夠快 |
| 最終整合 | Gemini Flash（較強推理） | 品質較好 |

### 8.4 Prompt 設計重點（規格層級）

1. **分群 Prompt**：輸入所有片段（附編號）→ 輸出 JSON：`{groups: [{name, summary, fragmentIds: []}]}`；要求：群數自動決定（3–8 群為佳）、單一群組不可過大、未分類片段歸入「其他」。
2. **衝突偵測 Prompt**：針對群內片段 → 輸出 `conflicts: [{a, b, reason, interpretation1, interpretation2}]`；與分群合併執行以省額度。
3. **整合 Prompt**：依成果型態提供不同系統指令（行動計畫 → 步驟+負責人+期限；企劃書 → 背景/目標/方案/預算；文章 → 標題/大綱/內文；報告 → 章節化；路線圖 → 階段+里程碑）。要求：全部片段內容不得遺漏、產出繁體中文、引用格式 `[n]`（對應片段編號）、衝突處以「兩種詮釋」呈現。
4. **溫度設定**：分群用低溫（0.2，穩定）、整合用中溫（0.6，創意）。

### 8.5 額度與錯誤處理

- 每次整合前顯示「約略消耗次數」提示（1 次分群 + 1 次整合）。
- 遇 Rate Limit / 429：顯示明確錯誤訊息，建議稍後再試或改用備援供應商。
- 大檔案（多張圖、大 PDF）分批送：圖片壓縮後再傳，PDF 拆頁。

---

## 9. 技術架構

- **形式**：純靜態網頁（HTML + CSS + JavaScript，ES Modules），**無框架、無建置步驟**、無套件管理器。
- **執行方式**：File System Access API 需要安全環境——透過 `python -m http.server` 或 `npx serve` 啟動本機伺服器（`file://` 直接開檔會受限）。
- **關鍵依賴**（盡量少的 CDN 函式庫）：
  - **pdf.js**（CDN）：PDF 解析。
  - 其餘一律原生 API（`MediaRecorder`、`fetch`、`File System Access API`、`IndexedDB` 或 localStorage）。
- **資料讀寫**：File System Access API 選資料夾 → 直接讀寫 `fragments/`、`index.json` 等（見 §7）。
- **樣式**：單一樣式表，RWD 響應式（手機可用），支援深色/淺色主題。

### 9.1 畫面規劃（UI）

| 畫面 | 內容 |
|---|---|
| **收件匣（主畫面）** | 頂部常駐快速輸入框＋錄音按鈕；下方為片段時間流 |
| **片段庫** | 全部片段，搜尋、標籤篩選、編輯/刪除、重複標示 |
| **拼圖工作台** | 選擇要整合的片段 → 選成果型態 → 「開始拼圖」→ **展示 AI 分群結果（群組卡片，可調整/確認）** → 確認後一鍵整合 |
| **成果檢視** | Markdown 渲染、引用來源面板、衝突標記、複製/匯出按鈕 |
| **設定** | API 金鑰、供應商切換、資料夾選擇、語言（預設繁中） |

### 9.2 視覺設計原則

- 拼圖隱喻：群組卡片以**拼圖塊樣式**呈現（圓角、嵌合邊緣），過渡動畫輕量。
- 主色調沉穩（如深藍/靛色系），重視階層與留白；所有按鈕有 hover 狀態與微互動。

---

## 10. 邊緣案例（Edge Cases）

| 情境 | 處理方式 |
|---|---|
| 片段只有一句話 | 完全支援（一句話就是合法片段） |
| 多個片段高度相似 | 整合時標示「相似群」，使用者自行決定是否合併 |
| 片段互相矛盾 | 標記衝突並提供**兩種詮釋**（F3.5） |
| 分群結果不理想 | 使用者可手動調整群組（移動片段）後重新整合 |
| 片段數量極多（>100） | 分批分群；UI 上提示數量；V2 可做階層分群 |
| 圖片內容理解失敗 | 標示該片段「AI 無法解析」，不阻斷整體流程 |
| 語音過長 | 錄音設上限（如 3 分鐘），過長提示分段 |
| API 額度用罄 / 429 | 明確錯誤訊息＋建議等候或切換供應商 |
| 無 API 金鑰時 | 仍可完整使用捕捉/管理功能；只有拼圖功能需要金鑰 |
| 瀏覽器不支援 File System Access API | 退回「匯出/匯入 JSON」模式（見 §12） |
| 網路離線 | 捕捉與管理可離線運作；拼圖需要連線 |
| 金鑰外洩風險 | 設定頁顯示風險警告；金鑰僅存 localStorage |

---

## 11. 非功能需求

- **免費**：全程零成本（AI 使用免費額度）。
- **隱私**：資料存本機；上傳至 AI API 的內容僅用於整合任務（使用者已知悉並接受，訪談確認）。
- **效能**：片段清單 1,000 筆內操作流暢；大檔案處理有進度提示。
- **可攜帶性**：整個資料夾可複製/備份/搬移；匯入匯出功能完整。
- **相容性**：Chrome/Edge 為主要目標（File System Access API）；手機瀏覽器可基本使用。

---

## 12. 開放問題（待確認）

1. **檔案讀寫方案**：File System Access API（Chrome/Edge 限定、直接操作資料夾）vs. 匯出/匯入檔案（全瀏覽器通用）？**規格暫定前者、後者為備援**。
2. **分享進階需求**：訪談選「匯出檔案」。是否未來需要「產生公開網頁連結」？（V2 候選）
3. **多語言片段**：輸入可能夾雜英文；輸出固定繁中，若片段全為英文是否改輸出英文？（暫定：一律繁中，除非使用者手動要求）
4. **單一供應商 vs 多供應商**：預設只做 Gemini；Groq/OpenRouter 是否要在一開始就做可切換的抽象層？
5. **PDF 是否要圖片型 PDF 的 OCR**：V1 只處理文字型 PDF，掃描圖片型 PDF 需要 OCR（免費 API 未必支援）。

---

## 13. 里程碑規劃（建議）

| 階段 | 內容 | 驗收標準 |
|---|---|---|
| **MVP（V1.0）** | 手動文字捕捉、片段管理（清單/編輯/刪除/搜尋）、拼圖工作台（選片段→選型態→分群展示→一鍵整合→成果檢視）、引用來源、衝突兩種詮釋、匯出 Markdown、Gemini 金鑰設定、資料夾讀寫 | 完成「捕捉 10 個片段 → 拼圖 → 產出行動計畫」全流程 |
| **V1.5** | 語音錄音轉文字（Groq Whisper）、圖片上傳與視覺理解、PDF 上傳與解析、重複標示 | 語音/圖片/PDF 三種來源可完整參與拼圖 |
| **V2.0（未來）** | 網頁摘錄自動抓取、公開網頁分享連結、多供應商抽象層、階層分群、成果範本 | 視使用者反饋決定優先序 |

---

## 14. 驗收標準（總）

1. 使用者可用至少**三種管道**（文字＋任一其他）收集片段，片段存為本機 Markdown/JSON 檔案。
2. 按「開始拼圖」後，AI 先展示**分群結果**，使用者確認後一鍵產出**指定型態**成果。
3. 成果為繁體中文、結構完整、**每個段落可追溯到來源片段**。
4. 互相矛盾的片段在成果中**標記衝突並提供兩種詮釋**。
5. 成果可**匯出 Markdown/PDF/純文字**。
6. 全程**不需要任何付費**，可於本機無後端運行。

---

*本文檔為訪談產出之規格草稿。下一步：確認 §12 開放問題後，即可開始實作 MVP。*
